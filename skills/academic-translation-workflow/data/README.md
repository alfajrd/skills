# data/

Per-client glossaries and style profiles, created automatically the first
time a new client is worked with. See `../references/data-schema.md` for the
schema. This directory is intentionally versioned alongside the skill so the
terminology history travels with it.
